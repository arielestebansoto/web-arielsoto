import React from 'react'

import { usePush } from '../../hooks/usePush'

export const Audiovisual = (props) => {

    usePush(props)
    
    return <h4>Section in construction <br/> redirecting...</h4>
}