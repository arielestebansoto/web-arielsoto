import styled from "styled-components";

export const Pointer = styled.div`
    position: absolute;
    width: 16px;
    height:16px;
    background: rgba(152, 224, 255, 1);
    filter: blur(6px);
    border-radius: 50%;
`